<site-context domain="trial-8689388.okta.com">

## Okta Admin Console — Site Context

**Base URL:** https://trial-8689388.okta.com
**Framework:** Standard HTML/CSS — no Vuetify. Use standard selectors.

### Authentication

**Auth type: manual_required** — OAuth2 PKCE redirect. The extension CANNOT automate Okta login.

- If the session has expired, the user is redirected to `/oauth2/v1/authorize?...`
- Tell the user: "Your Okta session has expired. Please log in, and I'll continue when you're back."
- Poll every few seconds for the authenticated state (URL contains `/admin/` and is NOT an OAuth authorize URL)
- Authenticated landing page: `/app/UserHome?session_hint=AUTHENTICATED` or `/admin/dashboard`
- Do NOT attempt to automate login — Okta may require MFA (phone push, Okta Verify, password). Wait for the user.

### Known Payloads

No validated payloads yet for Okta. Use DO mode inference for all requests.

### Page Context Map

| URL Pattern | What the user can do here |
|-------------|--------------------------|
| `/admin/dashboard` | Dashboard — org overview |
| `/admin/users` | Users list — add, deactivate, unlock, reset password |
| `/admin/user/{id}/profile` | User profile — edit attributes, groups, apps |
| `/admin/groups` | Groups list — create, manage membership |
| `/admin/apps/active` | Active applications |
| `/admin/app/{app}/instance/{id}/` | App assignment — manage users assigned to an app |
| `/admin/security/general` | Security policies overview |
| `/admin/access/authenticators` | MFA authenticators — Email, Okta Verify, Password, FastPass |
| `/admin/access/policies` | Global session and authentication policies |
| `/admin/customizations/branding` | Branding customization |
| `/admin/reports` | Reports — system log, imports |

**Discovered stats (as of 2026-04-03):**
- 7 users, 8219+ app catalog integrations
- MFA: Email, Okta Verify with FastPass, Password
- Custom admin roles available (RBAC)

### DOM Patterns

Okta Admin Console uses standard HTML. Key patterns:
- Look for `[data-se]` attributes for reliable targeting
- Standard `<button>`, `<input type="text">`, `<select>`, `<table>` elements
- Navigation: `[data-se='sidenav']` or `nav` with link text
- User table rows: `tr` with user data cells
- Action buttons: `button[data-se='...']` or buttons near table rows

</site-context>
