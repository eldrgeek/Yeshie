<site-context domain="trial-8689388.okta.com">

## Okta Admin Console — Site Context

**Base URL:** https://trial-8689388-admin.okta.com
**MyApps URL:** https://trial-8689388.okta.com (different subdomain — admin console is on -admin subdomain)
**Framework:** MUI (Material UI) — standard HTML/CSS with MuiButtonBase-root, MuiButton-root classes. No Vuetify.

### Authentication

**Auth type: manual_required** — OAuth2 PKCE redirect. The extension CANNOT automate Okta login.

- If the session has expired, the user is redirected to `/oauth2/v1/authorize?...`
- Tell the user: "Your Okta session has expired. Please log in, and I'll continue when you're back."
- Authenticated landing page: `/app/UserHome?session_hint=AUTHENTICATED` (on main domain) or `/admin/users` (on -admin subdomain)
- Do NOT attempt to automate login — Okta may require MFA. Wait for the user.

### Known Payloads

| # | Payload | Description | Required Params | Status |
|---|---------|-------------|-----------------|--------|
| 1 | `01-list-users.payload.json` | List all users with names, emails, and statuses | none | verified |
| 2 | `02-add-user.payload.json` | Create a new user in Okta | `first_name`, `last_name`, `email` | verified |
| 3 | `03-deactivate-user.payload.json` | Deactivate an active user account | `user_identifier` | verified |
| 4 | `04-list-groups.payload.json` | List all groups with member counts | none | verified |
| 5 | `05-list-apps.payload.json` | List all active SSO applications | none | verified |
| 6 | `06-view-system-log.payload.json` | View system audit log (last 7 days) with filtering | none | verified |
| 7 | `07-user-profile-actions.payload.json` | Navigate to user profile and perceive available lifecycle actions | `userId` | verified |
| 8 | `08-create-group.payload.json` | Create a new group in Okta | `group_name`, `group_description` (optional) | unverified |
| 9 | `09-add-user-to-group.payload.json` | Add a user to a group via user profile Groups tab | `userId`, `group_name` | unverified |
| 10 | `10-remove-user-from-group.payload.json` | Remove a user from a group | `userId`, `group_name` | unverified |
| 11 | `11-assign-app-to-user.payload.json` | Assign an application to a user | `userId`, `app_name` | unverified |
| 12 | `12-revoke-app-from-user.payload.json` | Revoke (deassign) an application from a user | `userId`, `app_name` | unverified |
| 13 | `13-unlock-user.payload.json` | Unlock a LOCKED_OUT user account | `userId` | unverified |
| 14 | `14-reset-password.payload.json` | Reset password for a user (sends reset email) | `userId` | unverified |

Payload path pattern: `~/Projects/yeshie/sites/okta/tasks/{filename}`

**Payload Keyword Mapping:**

| User says | Use this payload |
|-----------|-----------------|
| "list users", "show users", "who's in Okta", "how many users", "all users" | `01-list-users.payload.json` |
| "add user", "create user", "new user in Okta", "onboard" | `02-add-user.payload.json` |
| "deactivate", "remove from Okta", "disable user", "offboard" | `03-deactivate-user.payload.json` |
| "list groups", "show groups", "all groups", "group list" | `04-list-groups.payload.json` |
| "list apps", "show apps", "applications", "integrations" | `05-list-apps.payload.json` |
| "system log", "audit log", "logs", "events", "activity" | `06-view-system-log.payload.json` |
| "user profile", "show user", "user details", "user info" | `07-user-profile-actions.payload.json` |
| "create group", "new group", "make a group" | `08-create-group.payload.json` |
| "add user to group", "add to group", "join group" | `09-add-user-to-group.payload.json` |
| "remove from group", "delete from group", "leave group" | `10-remove-user-from-group.payload.json` |
| "assign app", "give access", "grant app", "app access" | `11-assign-app-to-user.payload.json` |
| "revoke app", "remove access", "deassign app" | `12-revoke-app-from-user.payload.json` |
| "unlock user", "unlock account", "unlock login" | `13-unlock-user.payload.json` |
| "reset password", "password reset", "forgot password" | `14-reset-password.payload.json` |

### Page Context Map

| URL Pattern | What the user can do here |
|-------------|--------------------------|
| `/admin/users` | Users list — add, deactivate, unlock, reset password, search |
| `/admin/user/profile/view/{id}` | User profile — edit attributes, deactivate, reset password |
| `/admin/groups` | Groups list — create, manage membership |
| `/admin/apps/active` | Active applications |
| `/admin/security/general` | Security policies overview |
| `/admin/reports` | Reports — system log, imports |

### DOM Patterns (MUI-based)

Okta Admin Console (trial) uses MUI (Material UI). Key patterns:
- **Read selector:** default `body` — the standard read captures buttons, links, inputs, tables
- **Search:** `textarea[placeholder*='Search for users']` — confirmed working
- **User table:** `table` with headers `[Person & username, Primary email, Status]` — rowCount=8 as of April 2026 (includes Yeshie Test user)
- **Add person:** `click_text` with text "Add person" — confirmed visible on `/admin/users`
- **User profile links:** `a[href*='/admin/user/profile/view/']` — one per user in table
- **Form inputs:** `input[name='profile.firstName']`, `input[name='profile.lastName']`, `input[name='profile.login']`, `input[name='profile.email']`
- **Submit button:** `input[type='submit']`
- **Error selectors:** `.o-form-input-error`, `.inlineError`, `[data-se='error-summary']`
- **Navigation tabs:** hash-based links (e.g., `#tab-apps`, `#tab-groups`, `#tab-account`)
- **More Actions dropdown:** `click_text("More Actions")` — contains lifecycle actions (Deactivate, Suspend, etc.)
- **Confirmation buttons:** `click_text("OK")` for deactivation confirm (not "Deactivate")
- **Groups list:** `table` with columns [Group name, People, Applications]
- **Add group:** `click_text` with text "Add group"
- **Applications list:** `table` with columns [App name, Status, Client ID]
- **Create App Integration:** `click_text` with text "Create App Integration"
- **Browse App Catalog:** link to `/admin/apps/add-app`
- **Assign Users to App:** link to `/admin/app/bulk-assign`
- **Status filter:** readonly textbox showing current filter value
- **Advanced search:** `click_text` with text "Advanced search ▾"

### Known Data (as of 2026-04-16)

- **Org:** embeddedsystemsresearch-trial-8689388
- **Admin user:** Michael Wolf (mike@embeddedsystemsresearch.org) — Active
- **8 users total:**
  - Yeshie Test (yeshietest@yeshietest.com) — Pending user action
  - Hermes AI (hermes@hermes.ai) — Pending user action
  - Perplexity AI (perplexity@embeddedsystemsresearch.org) — Pending user action
  - ChatGPT OpenAI (chatgpt@embeddedsystemsresearch.org) — Pending user action
  - Grok xAI (grok@embeddedsystemsresearch.org) — Pending user action
  - Gemini Google (gemini@embeddedsystemsresearch.org) — Pending user action
  - Claude Anthropic (claude@embeddedsystemsresearch.org) — Pending user action
  - Michael Wolf (mike@embeddedsystemsresearch.org) — Active
- **2 built-in groups:** Everyone (8 members), Okta Administrators
- **6 SSO apps:** Okta Admin Console, Okta Browser Plugin, Okta Dashboard, Okta Workflows, Okta Workflows OAuth, + 1 more (likely custom)

### IMPORTANT: Admin Subdomain

The Okta admin console is at `trial-8689388-admin.okta.com` — a SEPARATE subdomain from the user-facing `trial-8689388.okta.com`. Direct navigation to `/admin/users`, `/admin/dashboard` etc. on the main domain returns 404. Always use the `-admin` subdomain for admin console actions.

</site-context>
