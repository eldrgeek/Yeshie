{
  "_meta": {
    "task": "submit-prompt",
    "description": "Submit a prompt to Grok and capture the response.",
    "params": {
      "prompt": {
        "required": true,
        "description": "The prompt/question to send to Grok",
        "hint": "Can be any length. For multi-line, use newlines in the string."
      }
    },
    "selfImproving": true,
    "runCount": 0,
    "lastSuccess": null,
    "notes": "Grok uses Tiptap/ProseMirror contenteditable (not textarea). Use Input.insertText via CDP. Submit button: button[aria-label='Submit'] \u2014 stable. Response selector needs calibration after first run."
  },
  "params": {
    "prompt": "Say hello in exactly one sentence."
  },
  "abstractTargets": {
    "prompt_input": {
      "description": "Tiptap/ProseMirror contenteditable \u2014 confirmed on fresh Grok page",
      "fallbackSelectors": [
        "div.tiptap.ProseMirror[contenteditable='true']",
        ".ProseMirror[contenteditable='true']",
        "[contenteditable='true']"
      ]
    },
    "send_button": {
      "description": "Submit button \u2014 stable aria-label='Submit'",
      "anchors": {
        "ariaLabel": "Submit"
      },
      "fallbackSelectors": [
        "button[aria-label='Submit']",
        "button[aria-label*='Submit']"
      ]
    }
  },
  "chain": [
    {
      "stepId": "s1",
      "action": "wait_for",
      "target": "prompt_input",
      "state": {
        "visible": true
      },
      "timeout": 8000,
      "note": "wait for prompt_input before next step (was delay 800ms)"
    },
    {
      "stepId": "s2",
      "action": "type",
      "target": "prompt_input",
      "value": "{{prompt}}",
      "submit": true,
      "note": "ProseMirror contenteditable: Input.insertText + Enter key. submit:true fires Enter."
    },
    {
      "stepId": "s3",
      "action": "wait_for",
      "selector": "main, [role='main'], .v-main, .v-data-table, .application-main, #app, #root, [data-testid='app-root']",
      "timeout": 30000,
      "onTimeout": "continue",
      "note": "settle (was delay 30000ms); resolves on content root, proceeds on timeout \u2014 HAND-TUNE: LLM-generation, use a completion signal"
    },
    {
      "stepId": "s4",
      "action": "read",
      "candidates": [
        "[class*='markdown']:last-of-type",
        "[class*='markdown']"
      ],
      "store_as": "response",
      "note": "Confirmed 2026-04-06: [class*='markdown']:last-of-type captures Grok's answer (skips thinking preamble)"
    }
  ],
  "site": "grok.com",
  "mode": "exploratory"
}
