{
  "_meta": {
    "task": "issue-filter-by-assignee",
    "description": "Filter issues on a GitHub repository by assignee. Public repos accessible without login.",
    "selfImproving": true,
    "runCount": 0,
    "lastSuccess": null,
    "auth": { "type": "none_public_repo", "note": "Public repos accessible without login" },
    "params": {
      "owner": { "required": true, "description": "Repo owner (user or org)" },
      "repo": { "required": true, "description": "Repo name" },
      "assignee": { "required": true, "description": "GitHub username to filter by" }
    },
    "risk": "safe",
    "needs_live_verification": false
  },
  "runId": "github-issue-filter-by-assignee-{{timestamp}}",
  "mode": "verification",
  "site": "github.com",
  "params": {
    "owner": "{{owner}}",
    "repo": "{{repo}}",
    "assignee": "{{assignee}}"
  },
  "chain": [
    { "stepId": "s1", "action": "navigate", "url": "https://github.com/{{owner}}/{{repo}}/issues?q=is%3Aissue+is%3Aopen+assignee%3A{{assignee}}" },
    { "stepId": "s2", "action": "delay", "ms": 1500 },
    { "stepId": "s3", "action": "read", "store_as": "filtered_issues" }
  ]
}
